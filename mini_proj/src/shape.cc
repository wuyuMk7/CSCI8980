#include "shape.h"

// void Shape::draw() const 
// {

// }

// void Shape::move(const std::unordered_map<unsigned int, glm::vec3> &n_verts)
// {
//     for (auto it = n_verts.begin(); it != n_verts.end(); ++it)
//         if (it->first < _verts.size())
//             _verts[it->first] += it->second;
// }

// void Shape::moveTo(const std::unordered_map<unsigned int, glm::vec3> &n_verts)
// {
//     for (auto it = n_verts.begin(); it != n_verts.end(); ++it)
//         if (it->first < _verts.size())
//             _verts[it->first] = it->second;
// }